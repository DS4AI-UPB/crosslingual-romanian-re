# ─────────────────────────────────────────────────────────────
# Inference (running infer_classification.py / infer_e2e.py)
#
# The adapters were trained with Unsloth on top of Gemma 4 31B, which uses
# custom 4-bit linear layers that plain peft does not yet wrap. The inference
# scripts therefore load the adapters through Unsloth, the same stack used for
# training and evaluation in the paper.
# ─────────────────────────────────────────────────────────────
unsloth
torch>=2.4
transformers>=4.45
peft>=0.13
bitsandbytes>=0.43
accelerate>=0.34
sentencepiece
scikit-learn        # for the evaluation / significance scripts

# ─────────────────────────────────────────────────────────────
# Reproducing training (optional)
# ─────────────────────────────────────────────────────────────
# trl
# datasets
# vllm                 # optional, for faster batched inference

# Notes:
# - The base model google/gemma-4-31b-it is gated; accept its license on the
#   Hugging Face Hub and log in with `hf auth login` before downloading.
# - 4-bit inference of the 31B model needs roughly 25 GB of GPU memory.
# - The exact paper environment used unsloth 2026.4.8, transformers 5.5.0 and
#   torch 2.10.0+cu128 on a single NVIDIA A100 40GB; the looser pins above are
#   for portability. If you hit a version conflict, pin to those versions.